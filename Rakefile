# lib/tasks/dev.rake

# Environment specific tasks
namespace :dev do 
    namespace :test do
        current_env = 'dev'
        desc "Onboard all backend services to a CI/CD pipeline"
        task :helloworld do
            puts "Hello world..."
        end
    end
end